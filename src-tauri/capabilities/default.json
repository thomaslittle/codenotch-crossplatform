{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for Codenotch windows and events",
  "windows": ["notch", "tooltip", "settings", "context-menu"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
